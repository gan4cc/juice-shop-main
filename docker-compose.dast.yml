services:
  app:
    image: ${IMAGE_TAG}

  zap:
    image: ghcr.io/zaproxy/zaproxy:stable
    depends_on:
      - app
    command: >
      zap-baseline.py
      -t http://app:3000
      -r zap-report.html
    volumes:
      - ./zap-reports:/zap/wrk
